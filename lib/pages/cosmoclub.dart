import 'package:flutter/material.dart';

class CosmoClub extends StatelessWidget {
  const CosmoClub({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
